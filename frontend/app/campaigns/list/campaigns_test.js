'use strict';

describe('mailtasticApp.campaigns module', function() {

  beforeEach(module('mailtasticApp.campaigns'));

  describe('campaigns controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('campaignsCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});